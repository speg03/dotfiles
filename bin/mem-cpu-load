#!/usr/bin/env zsh

### Display Memory/CPU usage for tmux

if ! type tmux-mem-cpu-load &>/dev/null; then
    echo "tmux-mem-cpu-load is not installed"
    exit 0
fi

tmux-mem-cpu-load -cq -i5 -t1 -a0
