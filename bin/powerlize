#!/usr/bin/env zsh

### Powerline string

str=$(cat)

fg_color=$1
bg_color=$2

if [[ $fg_color && $fg_color != '-' ]]; then
    str="%F{$fg_color}$str%f"
fi

echo -n "%F{$fg_color}%K{$bg_color} $str %k%f%F{$bg_color}"$'\UE0B0'"%f"
