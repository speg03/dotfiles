#!/usr/bin/env zsh

### Colorize string

str=$(cat)

fg_color=$1
bg_color=$2

if [[ $fg_color && $fg_color != '-' ]]; then
    str="%F{$fg_color}$str%f"
fi

if [[ $bg_color ]]; then
    str="%K{$bg_color}$str%k"
fi

echo -n "$str"
