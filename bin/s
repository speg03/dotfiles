#!/usr/bin/env bash

### Search file contents

ag --noheading . |
    fzf -d: --tac -q "$*" \
        --bind 'enter:execute: less +{2} {1}' \
        --bind 'ctrl-v:execute: pygmentize {1} | less +{2}' \
        --bind 'ctrl-m:execute: $EDITOR +{2} {1}'
