[core]
	quotepath = false
[ghq]
	root = ~/.local/lib/go/src
	root = ~/.local/src
[include]
	path = config.local
[pager]
	branch = cat

[tig "bind"]
	main = F !git commit --fixup %(commit)
	main = B !git rebase -i --autosquash %(commit)
